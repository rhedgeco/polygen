# Initial ignore for everything.
# I generally like to do this at the project root as a directory whitelist
# This makes it so that specific includes are not necessary for any IDE
/*
/*/

# whitelist important git files
!.gitignore
!.gitattributes
!LICENSE.md
!README.md

# whitelist project files
!/examples/
!/generators/
!/polygen-proc/
!/src/
!Cargo.toml

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb
